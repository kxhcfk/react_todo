import styles from './Logo.module.scss'

const Logo = () => {
	return (
		<div className={styles.root}>
			<span>t</span>
			<span>o</span>
			<span>d</span>
			<span>o</span>
		</div>
	);
};

export default Logo;